---
# Playbook zur Konfiguration aller Server auf denen Arbeitsmittel liegen.

 - hosts: ffms-arbeitsmittel
   remote_user: root
   roles:
    - { role: hostname, tags: "hostname"}
    - { role: common, tags: "common"}

